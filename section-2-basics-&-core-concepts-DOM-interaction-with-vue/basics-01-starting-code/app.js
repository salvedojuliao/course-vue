const app = Vue.createApp({
    data(){
        return {// it aways return an object
            courseGoul: 'Finish the course and learn Vue!'  // the name here it's up to me;
        }; 
    }
});

app.mount('#user-goal'); 